# 로컬 데이터 - 접근키·검색량 캐시·덤프. 시험이 돌 때마다 통째로 지워지는 자리다.
# 순위 기록은 여기 두지 않는다(rank-log/ 로 뺐다) - 지워졌다가 그대로 커밋되면
# 그동안 쌓은 것이 통째로 없어진다.
data/

# 의존성 없음 — 혹시 생기면 무시
node_modules/

# 환경변수
.env

# 지점 사실 파일 - 상담 전화번호(010)가 들어 있어 공개 저장소에 올리지 않는다.
# 답글에 쓸 사실은 도구가 네이버에서 직접 긁어온다(진단 → 수집). 그쪽이 안 낡는다.
.claude/skills/*/references/stores.md
